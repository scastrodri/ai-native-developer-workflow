# Groomed Task Template

## Goal

One sentence describing the outcome of this task.

## Acceptance Criteria

- [ ] Specific, testable criterion.
- [ ] Specific, testable criterion.
- [ ] Specific, testable criterion.

## Out of Scope

- Work explicitly not included in this task.

## Constraints

- Relevant decisions from `_docs/architecture.md`.
- Product boundaries from `_docs/plan.md`.

## Done Means

- Real command from `AGENTS.md`, for example `cd backend && uv run pytest`.
- Real command from `AGENTS.md`, for example `cd frontend && npm test`.
- Any task-specific verification command from `AGENTS.md`.
